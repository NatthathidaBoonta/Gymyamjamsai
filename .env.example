# Docker Compose Environment Variables
# Copy this file to .env and adjust values as needed

# MySQL
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=gymyamjamsai
MYSQL_USER=gymuser
MYSQL_PASSWORD=gympassword

# These will be mounted into backend/.env and frontend/.env by docker-compose
# Edit ./backend/.env and ./frontend/.env directly for additional customization
