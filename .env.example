# Docker Compose Environment Variables
# Copy this file to .env and adjust values as needed for docker-compose

# MySQL Configuration
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=gymyamjamsai
MYSQL_USER=gymyam_user
MYSQL_PASSWORD=gymyam_password

# Note: These values will be passed to docker-compose and must match backend/.env
# For backend local development (outside docker), see backend/.env
# For frontend env vars, see frontend/.env
